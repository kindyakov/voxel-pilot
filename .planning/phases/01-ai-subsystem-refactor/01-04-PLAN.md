---
phase: 01-ai-subsystem-refactor
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - src/ai/client.ts
  - src/ai/tools.ts
  - src/ai/loop.ts
  - src/hsm/context.ts
  - src/hsm/machine.ts
  - src/tests/core/ChatClient.test.ts
  - src/tests/ai/windowRuntime.test.ts
  - src/tests/ai/agentLoop.test.ts
autonomous: true
requirements:
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "Facade entrypoints remain stable for old imports while internal modules own the logic."
    - "HSM imports use stable AI contracts instead of depending on tool-god-file ownership where safe."
    - "Targeted tests, type-check, and build all pass after the refactor waves complete."
  artifacts:
    - path: "src/ai/client.ts"
      provides: "Thin facade export surface"
      contains: "export {"
    - path: "src/ai/tools.ts"
      provides: "Thin facade export surface"
      contains: "export {"
    - path: "src/ai/loop.ts"
      provides: "Thin facade export surface"
      contains: "export {"
  key_links:
    - from: "src/hsm/context.ts"
      to: "src/ai/contracts/execution.ts"
      via: "PendingExecution contract import"
      pattern: "contracts/execution"
    - from: "src/hsm/machine.ts"
      to: "src/ai/contracts/agentTurn.ts"
      via: "AgentTurnResult contract import"
      pattern: "contracts/agentTurn"
---

<objective>
Consolidate the refactor by keeping entry facades thin, switching stable consumers to contract imports where safe, and running full regression verification.

Purpose: Finish the structural split without leaving ownership confusion or unverified integration edges.
Output: Thin AI facades, optional HSM contract-import cleanup, and complete verification evidence for the phase.
</objective>

<execution_context>
@D:/Developer/Minecraft_bot/.codex/get-shit-done/workflows/execute-plan.md
@D:/Developer/Minecraft_bot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-subsystem-refactor/01-CONTEXT.md
@.planning/phases/01-ai-subsystem-refactor/01-01-SUMMARY.md
@.planning/phases/01-ai-subsystem-refactor/01-02-SUMMARY.md
@.planning/phases/01-ai-subsystem-refactor/01-03-SUMMARY.md
@src/hsm/context.ts
@src/hsm/machine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint stable consumers to contract modules where the ownership is now explicit</name>
  <files>src/hsm/context.ts, src/hsm/machine.ts</files>
  <read_first>src/hsm/context.ts, src/hsm/machine.ts, src/ai/contracts/execution.ts, src/ai/contracts/agentTurn.ts</read_first>
  <action>Update `src/hsm/context.ts` and `src/hsm/machine.ts` to import `PendingExecution`, `AgentTurnResult`, and other stable contracts from `src/ai/contracts/*` if doing so reduces ownership ambiguity without breaking compatibility. Do not rewrite HSM behavior or state topology. If a given import must stay on the facade for compatibility reasons, leave it and document that choice in the SUMMARY.</action>
  <verify>rtk npx tsx --test src/tests/ai/agentLoop.test.ts</verify>
  <acceptance_criteria>
    - `src/hsm/context.ts` imports `PendingExecution` from a contracts path or remains explicitly on the facade with a documented reason in SUMMARY
    - `src/hsm/machine.ts` imports `AgentTurnResult` from a contracts path or remains explicitly on the facade with a documented reason in SUMMARY
  </acceptance_criteria>
  <done>Stable consumers use explicit contracts where safe, without rewriting HSM behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the three AI entry files as thin facades and remove leftover duplicated ownership</name>
  <files>src/ai/client.ts, src/ai/tools.ts, src/ai/loop.ts</files>
  <read_first>src/ai/client.ts, src/ai/tools.ts, src/ai/loop.ts</read_first>
  <action>Audit `src/ai/client.ts`, `src/ai/tools.ts`, and `src/ai/loop.ts` after the prior waves. Remove any leftover implementation bodies or duplicated helper ownership so each file is a facade-style module that only re-exports the stable surface or defines the thinnest necessary wrapper. Preserve all public export names currently used by tests and HSM.</action>
  <verify>rtk npm run type-check</verify>
  <acceptance_criteria>
    - `src/ai/client.ts` does not contain `class OpenAIResponsesClient`
    - `src/ai/tools.ts` does not contain `switch (name)`
    - `src/ai/loop.ts` does not contain `validateExecutionTool`
    - all three files contain `export {`
  </acceptance_criteria>
  <done>The AI entry files are thin facades with no leftover god-file ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Run full phase regression verification and record the final compatibility evidence</name>
  <files>src/tests/core/ChatClient.test.ts, src/tests/ai/windowRuntime.test.ts, src/tests/ai/agentLoop.test.ts</files>
  <read_first>src/tests/core/ChatClient.test.ts, src/tests/ai/windowRuntime.test.ts, src/tests/ai/agentLoop.test.ts, package.json</read_first>
  <action>Run the targeted AI regression suites plus full type-check and build. Use the exact verification commands listed below. If any command fails, fix compatibility regressions within this plan instead of deferring them. Record the commands and outcomes in the SUMMARY so later verification has concrete evidence.</action>
  <verify>rtk npx tsx --test src/tests/core/ChatClient.test.ts && rtk npx tsx --test src/tests/ai/windowRuntime.test.ts && rtk npx tsx --test src/tests/ai/agentLoop.test.ts && rtk npm run type-check && rtk npm run build</verify>
  <acceptance_criteria>
    - `rtk npx tsx --test src/tests/core/ChatClient.test.ts` exits 0
    - `rtk npx tsx --test src/tests/ai/windowRuntime.test.ts` exits 0
    - `rtk npx tsx --test src/tests/ai/agentLoop.test.ts` exits 0
    - `rtk npm run type-check` exits 0
    - `rtk npm run build` exits 0
  </acceptance_criteria>
  <done>Compatibility evidence exists for tests, type-check, and build after the refactor.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk npx tsx --test src/tests/core/ChatClient.test.ts`
- [ ] `rtk npx tsx --test src/tests/ai/windowRuntime.test.ts`
- [ ] `rtk npx tsx --test src/tests/ai/agentLoop.test.ts`
- [ ] `rtk npm run type-check`
- [ ] `rtk npm run build`
</verification>

<success_criteria>

- All tasks completed
- AI entrypoints remain thin facades
- Stable HSM consumers use explicit AI contracts where safe
- Targeted tests, type-check, and build all pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ai-subsystem-refactor/01-04-SUMMARY.md`
</output>
