---
phase: 01-ai-subsystem-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/ai/loop.ts
  - src/ai/contracts/agentTurn.ts
  - src/ai/loop/runAgentTurn.ts
  - src/ai/loop/grounding.ts
  - src/ai/loop/validation.ts
  - src/ai/loop/policy.ts
  - src/ai/loop/transcript.ts
  - src/tests/ai/agentLoop.test.ts
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "`runAgentTurn` orchestration is owned by a dedicated loop module instead of the facade file."
    - "Grounding, validation, retry limits, and transcript helpers are owned by dedicated loop modules."
    - "src/ai/loop.ts remains a backward-compatible facade for existing HSM and test imports."
  artifacts:
    - path: "src/ai/contracts/agentTurn.ts"
      provides: "Shared runAgentTurn input and output contracts"
      contains: "export type AgentTurnResult"
    - path: "src/ai/loop/runAgentTurn.ts"
      provides: "Loop orchestration entrypoint"
      contains: "export const runAgentTurn"
    - path: "src/ai/loop/grounding.ts"
      provides: "Grounded fact collection and indexing"
      contains: "collectGroundedFacts"
    - path: "src/ai/loop/validation.ts"
      provides: "Inline and execution validation"
      contains: "validateExecutionTool"
  key_links:
    - from: "src/ai/loop/runAgentTurn.ts"
      to: "src/ai/loop/validation.ts"
      via: "execution and inline validation calls"
      pattern: "validateExecutionTool"
    - from: "src/ai/loop/runAgentTurn.ts"
      to: "src/ai/loop/grounding.ts"
      via: "grounded fact collection"
      pattern: "collectGroundedFacts"
---

<objective>
Split `runAgentTurn` orchestration from contracts, grounding, validation, and loop policy ownership.

Purpose: Remove business rules and policy helpers from the `src/ai/loop.ts` god file while preserving behavior and HSM compatibility.
Output: Dedicated loop modules and a thin `src/ai/loop.ts` facade.
</objective>

<execution_context>
@D:/Developer/Minecraft_bot/.codex/get-shit-done/workflows/execute-plan.md
@D:/Developer/Minecraft_bot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-subsystem-refactor/01-CONTEXT.md
@.planning/phases/01-ai-subsystem-refactor/01-01-SUMMARY.md
@.planning/phases/01-ai-subsystem-refactor/01-02-SUMMARY.md
@src/ai/loop.ts
@src/tests/ai/agentLoop.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract agent turn contracts and grounded fact ownership</name>
  <files>src/ai/contracts/agentTurn.ts, src/ai/loop/grounding.ts</files>
  <read_first>src/ai/loop.ts, src/tests/ai/agentLoop.test.ts, src/ai/taskContext.ts</read_first>
  <action>Create `src/ai/contracts/agentTurn.ts` for the current `AgentTurnInput` and `AgentTurnResult` contracts, reusing the same shapes and field names now exposed from `src/ai/loop.ts`. Create `src/ai/loop/grounding.ts` for `GroundedTurnFacts`, grounded reference indexing, entity name-type pairing, window compatibility checks, `hasGroundedReferences`, and `collectGroundedFacts`. Preserve the current grounded-fact semantics exactly, including how `memory_read`, `inspect_blocks`, `inspect_entities`, and `inspect_window` populate capabilities.</action>
  <verify>rtk npx tsx --test src/tests/ai/agentLoop.test.ts</verify>
  <acceptance_criteria>
    - `src/ai/contracts/agentTurn.ts` contains `export interface AgentTurnInput`
    - `src/ai/contracts/agentTurn.ts` contains `export type AgentTurnResult`
    - `src/ai/loop/grounding.ts` contains `export const collectGroundedFacts`
    - `src/ai/loop/grounding.ts` contains `export const hasGroundedReferences`
  </acceptance_criteria>
  <done>Turn contracts and grounded fact ownership are extracted without changing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extract loop validation, policy, transcript helpers, and orchestration module</name>
  <files>src/ai/loop/validation.ts, src/ai/loop/policy.ts, src/ai/loop/transcript.ts, src/ai/loop/runAgentTurn.ts</files>
  <read_first>src/ai/loop.ts, src/ai/contracts/agentTurn.ts, src/ai/loop/grounding.ts, src/ai/client.ts, src/ai/tools.ts</read_first>
  <action>Create `src/ai/loop/validation.ts` for `validateInlineTool` and `validateExecutionTool`, including stonecutter rejection, grounded position checks, `mine_resource` count validation, and `follow_entity` grounding rules. Create `src/ai/loop/policy.ts` for `MAX_INLINE_TOOL_ROUNDS`, `MAX_MODEL_RETRIES`, and `parseSubGoal`. Create `src/ai/loop/transcript.ts` for transcript-friendly helpers such as execution signature generation. Create `src/ai/loop/runAgentTurn.ts` that orchestrates snapshot building, model calls, tool dispatch, retry handling, finish handling, and transcript logging while importing the extracted contracts and helpers instead of owning them inline.</action>
  <verify>rtk npx tsx --test src/tests/ai/agentLoop.test.ts</verify>
  <acceptance_criteria>
    - `src/ai/loop/validation.ts` contains `export const validateExecutionTool`
    - `src/ai/loop/policy.ts` contains `export const MAX_INLINE_TOOL_ROUNDS`
    - `src/ai/loop/runAgentTurn.ts` contains `export const runAgentTurn`
    - `src/ai/loop/runAgentTurn.ts` contains `createAgentClient`
  </acceptance_criteria>
  <done>Loop validation, policy, transcript helpers, and orchestration are separated into dedicated modules.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite src/ai/loop.ts as a facade and preserve agent loop parity</name>
  <files>src/ai/loop.ts</files>
  <read_first>src/ai/loop.ts, src/ai/contracts/agentTurn.ts, src/ai/loop/runAgentTurn.ts</read_first>
  <action>Replace the old `src/ai/loop.ts` implementation with re-exports from `src/ai/contracts/agentTurn.ts` and `src/ai/loop/runAgentTurn.ts`. Keep `runAgentTurn`, `AgentTurnInput`, and `AgentTurnResult` available from the old entrypoint so HSM and tests do not need immediate import path changes. Verify agent loop parity after the facade rewrite.</action>
  <verify>rtk npx tsx --test src/tests/ai/agentLoop.test.ts && rtk npm run type-check</verify>
  <acceptance_criteria>
    - `src/ai/loop.ts` contains `export { runAgentTurn }`
    - `src/ai/loop.ts` contains `export type { AgentTurnInput, AgentTurnResult }`
    - `src/ai/loop.ts` does not contain `for (let round = 0;`
  </acceptance_criteria>
  <done>`src/ai/loop.ts` is a thin facade and targeted agent loop tests still pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk npx tsx --test src/tests/ai/agentLoop.test.ts`
- [ ] `rtk npm run type-check`
</verification>

<success_criteria>

- All tasks completed
- Grounding, validation, policy, and orchestration ownership moves out of the loop facade
- `src/ai/loop.ts` remains a backward-compatible facade
- Agent loop regression coverage still passes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ai-subsystem-refactor/01-03-SUMMARY.md`
</output>
