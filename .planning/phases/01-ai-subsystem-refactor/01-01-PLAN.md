---
phase: 01-ai-subsystem-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/client.ts
  - src/ai/contracts/agentClient.ts
  - src/ai/client/factory.ts
  - src/ai/client/responsesClient.ts
  - src/ai/client/chatClient.ts
  - src/ai/client/parsers.ts
  - src/tests/core/ChatClient.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04
user_setup: []
must_haves:
  truths:
    - "The AI client contract is owned by a dedicated contracts module."
    - "Provider-specific Responses and chat adapters are implemented outside src/ai/client.ts."
    - "src/ai/client.ts remains a backward-compatible facade for existing imports."
  artifacts:
    - path: "src/ai/contracts/agentClient.ts"
      provides: "Shared AI client request and response contracts"
      contains: "export interface AgentModelClient"
    - path: "src/ai/client/factory.ts"
      provides: "Provider-aware client factory"
      contains: "export const createAgentClient"
    - path: "src/ai/client.ts"
      provides: "Backward-compatible facade"
      contains: "export {"
  key_links:
    - from: "src/ai/client/factory.ts"
      to: "src/ai/client/chatClient.ts"
      via: "OpenAICompatibleChatClient selection"
      pattern: "OpenAICompatibleChatClient"
    - from: "src/ai/client/factory.ts"
      to: "src/ai/client/responsesClient.ts"
      via: "OpenAIResponsesClient selection"
      pattern: "OpenAIResponsesClient"
---

<objective>
Extract AI client contracts, parsing helpers, provider adapters, and factory ownership out of `src/ai/client.ts`.

Purpose: Remove transport and parser coupling from the client facade before touching tools and loop code.
Output: Dedicated `contracts/agentClient.ts`, `client/parsers.ts`, `client/chatClient.ts`, `client/responsesClient.ts`, `client/factory.ts`, plus a thin `src/ai/client.ts` facade.
</objective>

<execution_context>
@D:/Developer/Minecraft_bot/.codex/get-shit-done/workflows/execute-plan.md
@D:/Developer/Minecraft_bot/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-subsystem-refactor/01-CONTEXT.md
@src/ai/client.ts
@src/tests/core/ChatClient.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared agent client contracts and parser helpers</name>
  <files>src/ai/contracts/agentClient.ts, src/ai/client/parsers.ts</files>
  <read_first>src/ai/client.ts, src/tests/core/ChatClient.test.ts</read_first>
  <action>Create `src/ai/contracts/agentClient.ts` and move the existing `ParsedToolCall`, `ParsedFunctionCallOutputItem`, `ParsedToolResponse`, `CreateResponseResult`, `AgentResponseRequest`, and `AgentModelClient` contracts into it. Create `src/ai/client/parsers.ts` and move `parseArguments`, `extractTextContent`, `mapParsedToolCalls`, `mapChatToolCalls`, `toChatTools`, `serializeChatInput`, and `getFirstChatChoiceMessage` into it without changing their signatures or return shapes.</action>
  <verify>rtk npx tsx --test src/tests/core/ChatClient.test.ts</verify>
  <acceptance_criteria>
    - `src/ai/contracts/agentClient.ts` contains `export interface AgentModelClient`
    - `src/ai/contracts/agentClient.ts` contains `export interface AgentResponseRequest`
    - `src/ai/client/parsers.ts` contains `export const parseArguments`
    - `src/ai/client/parsers.ts` contains `export const mapChatToolCalls`
  </acceptance_criteria>
  <done>Contracts and parser helpers are extracted into dedicated modules with unchanged behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Move concrete client adapters and factory into dedicated modules</name>
  <files>src/ai/client/chatClient.ts, src/ai/client/responsesClient.ts, src/ai/client/factory.ts</files>
  <read_first>src/ai/client.ts, src/ai/contracts/agentClient.ts, src/ai/client/parsers.ts</read_first>
  <action>Create `src/ai/client/responsesClient.ts` for `OpenAIResponsesClient`, `src/ai/client/chatClient.ts` for `OpenAICompatibleChatClient`, and `src/ai/client/factory.ts` for `createAgentClient`. Keep provider selection identical: `routerai`, `openrouter`, and `openai_compatible` must still map to `OpenAICompatibleChatClient`, and all other providers must still map to `OpenAIResponsesClient`. Preserve constructor option names and default config usage.</action>
  <verify>rtk npx tsx --test src/tests/core/ChatClient.test.ts</verify>
  <acceptance_criteria>
    - `src/ai/client/responsesClient.ts` contains `export class OpenAIResponsesClient`
    - `src/ai/client/chatClient.ts` contains `export class OpenAICompatibleChatClient`
    - `src/ai/client/factory.ts` contains `case 'routerai'`
    - `src/ai/client/factory.ts` contains `export const createAgentClient`
  </acceptance_criteria>
  <done>Client adapters and provider selection are isolated from the facade.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite src/ai/client.ts as a backward-compatible facade and verify parity</name>
  <files>src/ai/client.ts</files>
  <read_first>src/ai/client.ts, src/ai/contracts/agentClient.ts, src/ai/client/factory.ts, src/ai/client/chatClient.ts, src/ai/client/responsesClient.ts</read_first>
  <action>Replace the old implementation in `src/ai/client.ts` with re-exports from `src/ai/contracts/agentClient.ts`, `src/ai/client/chatClient.ts`, `src/ai/client/responsesClient.ts`, and `src/ai/client/factory.ts`. Keep the old export names available from `src/ai/client.ts` so existing imports do not change. Run targeted regression checks after the facade rewrite.</action>
  <verify>rtk npx tsx --test src/tests/core/ChatClient.test.ts && rtk npm run type-check</verify>
  <acceptance_criteria>
    - `src/ai/client.ts` contains `export { createAgentClient }`
    - `src/ai/client.ts` contains `export { OpenAICompatibleChatClient, OpenAIResponsesClient }`
    - `src/ai/client.ts` does not contain `class OpenAIResponsesClient`
    - `src/ai/client.ts` does not contain `class OpenAICompatibleChatClient`
  </acceptance_criteria>
  <done>`src/ai/client.ts` is a thin facade and the targeted client regression checks still pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk npx tsx --test src/tests/core/ChatClient.test.ts`
- [ ] `rtk npm run type-check`
</verification>

<success_criteria>

- All tasks completed
- Client contracts, adapters, parsers, and factory are isolated into dedicated modules
- `src/ai/client.ts` remains a backward-compatible facade
- No behavior regressions in targeted client tests
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ai-subsystem-refactor/01-01-SUMMARY.md`
</output>
